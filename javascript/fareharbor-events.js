window.addEventListener('fareharbor.ready', function () {
    // Get the sent data
  console.log('here');

    // If you encode the message in JSON before sending them,
    // then decode here
    // const decoded = JSON.parse(data);
});
